# Next.js Pages Router placeholder

This folder intentionally exists so Next.js does not treat `src/pages` as the Pages Router.

The app uses the App Router from `/app`, while `src/pages` is reserved for the FSD `pages` layer.
